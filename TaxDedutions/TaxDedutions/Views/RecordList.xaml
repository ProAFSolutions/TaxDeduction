<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaxDedutions.RecordList"
   xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms">
  <ContentPage.Content>
      
    <ScrollView>
      <StackLayout Padding="20">
        <Label Text="Invoices" FontSize="35" FontAttributes="Bold" HorizontalOptions="CenterAndExpand"/>

        <ListView x:Name="listView" ItemSelected="OnItemSelected" ItemsSource="{Binding Records}">
          <ListView.ItemTemplate>
            <DataTemplate>

              <ImageCell Text="{Binding Name}" Detail="{Binding Description}" ImageSource="{Binding Image}">
                
              </ImageCell>
              
              <!--<ViewCell>
                <StackLayout Padding="20,0,0,0" HorizontalOptions="StartAndExpand" Orientation="Horizontal">
                  <StackLayout Orientation="Vertical">
                   
                  </StackLayout>
                  
                  
                  <Label Text="{Binding Name}" FontSize="18" VerticalTextAlignment="Center" />
                  --><!--<Image Source="icon.png" />--><!--
                </StackLayout>
              </ViewCell>-->
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
        
        </StackLayout>
    </ScrollView>
  </ContentPage.Content>
</ContentPage>